import React from 'react';
import classNames from 'classnames';

function {componentName}({ className, ...rest }) {
  return <div className={classNames(className)} {...rest} />;
}

export default {componentName};
